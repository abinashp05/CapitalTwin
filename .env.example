GEMINI_API_KEY=
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=

# Email OTP login (Resend: https://resend.com — free tier works for demos)
RESEND_API_KEY=
RESEND_FROM=CapitalTwin <onboarding@resend.dev>
# Dev mode returns the code in the API response when no email key is set.
# Set to false in production. Values: true | false | (unset = auto)
OTP_DEV_MODE=
# Override the built-in demo emails with a real inbox to receive codes:
DEMO_SUPPLIER_EMAIL=
DEMO_LENDER_EMAIL=
DEMO_ADMIN_EMAIL=
